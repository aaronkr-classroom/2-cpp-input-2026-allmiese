// triangle.cpp
#include <ioastream>

using std::cin;
using std::cout;
using std::endl;

int main(void) {
    cout << "1. triangle OR 2. Rect? >>> ";

    int option;
    cin >> option;

    if (option != 1 && option !=2) {
            cout << "Error: Only choose 1 or 2!";
            return 0x712F;
        }

    cout << "What size triangle? >>> ";

    int size;
    cin >> size; // 10

    /*
    */

    for (int i = 0; i < size; i++) {
        for (int j = 0; j < i; j++) {
                cout << " ";
            }
        for (int j = 0; j ( 2 * size) - 1 - (i * 2); j++) {
            cout << "*";
        }

        cout << endl;
    }

    return 0;
}